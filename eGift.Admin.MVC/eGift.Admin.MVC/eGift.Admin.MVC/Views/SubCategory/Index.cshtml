@model eGift.Admin.MVC.Models.ListViewModels.SubCategoryListViewModel

@{
    ViewData["Title"] = "Sub Category";
    ViewData["SubTitle"] = "List";
    int index = 0;
}

<h1>@ViewData["Title"]</h1>
<h4>@ViewData["SubTitle"]</h4>
<hr />

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table id="sub-category-table" class="table  table-striped table-hover">
    <thead class="table-dark">
        <tr>
            <th>#</th>
            <th>
                @Html.DisplayNameFor(model => model.SubCategoryModel.SubCategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SubCategoryModel.CategoryName)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.SubCategoryList)
        {
            index++;
            <tr>
                <td>
                    @index
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SubCategoryName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CategoryName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID" title="Edit" class="text-decoration-none me-2">
                        <i class="fa-solid fa-pen text-dark"></i>
                    </a>
                    <a asp-action="Details" asp-route-id="@item.ID" title="Details" class="text-decoration-none me-2">
                        <i class="fa-solid fa-eye text-dark"></i>
                    </a>
                    <a asp-action="Delete" asp-route-id="@item.ID" title="Delete" class="text-decoration-none">
                        <i class="fa-solid fa-trash text-dark"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<script src="~/js/pages/sub-category.js"></script>